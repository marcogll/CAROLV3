:80 {
	encode gzip zstd

	# API and webhooks → Python backend
	handle /api/* {
		reverse_proxy app:8000
	}
	handle /webhook/* {
		reverse_proxy app:8000
	}

	# Static files
	handle {
		root * /srv/web
		file_server
		try_files {path} /carol_platform.html
	}
}
